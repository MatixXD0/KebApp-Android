package pl.kul.kebapp.ui.theme

import androidx.compose.ui.graphics.Color

// Light theme colors
val LightPrimary = Color(0xFF1976D2)      // Intensywny niebieski (Blue 700)
val LightOnPrimary = Color(0xFFFFFFFF)    // Biały tekst na niebieskim tle
val LightBackground = Color(0xFFFFFFFF)   // Białe tło
val LightSurface = Color(0xFFFFFFFF)      // Powierzchnia o jasnym tle
val LightOnBackground = Color(0xFF000000)   // Czarny tekst na tle
val LightOnSurface = Color(0xFF000000)      // Czarny tekst na powierzchni
val LightError = Color(0xFFD32F2F)          // Czerwony błąd
val LightOnError = Color(0xFFFFFFFF)        // Biały tekst na czerwonym tle
val LightSecondary = Color(0xFF64B5F6)      // Jaśniejszy odcień niebieskiego
val LightOnSecondary = Color(0xFF000000)    // Czarny tekst na odcieniu niebieskiego

// Dark theme colors
val DarkPrimary = Color(0xFF90CAF9)         // Jaśniejszy niebieski (Blue 200) dla kontrastu z ciemnym tłem
val DarkOnPrimary = Color(0xFF000000)       // Ciemny tekst na jaśniejszym niebieskim tle
val DarkBackground = Color(0xFF121212)      // Ciemne tło
val DarkSurface = Color(0xFF121212)         // Powierzchnia o ciemnym tle
val DarkOnBackground = Color(0xFFFFFFFF)    // Biały tekst na ciemnym tle
val DarkOnSurface = Color(0xFFFFFFFF)       // Biały tekst na powierzchni
val DarkError = Color(0xFFEF5350)           // Ciepły czerwony błąd
val DarkOnError = Color(0xFF000000)         // Ciemny tekst na błędzie
val DarkSecondary = Color(0xFF42A5F5)       // Średni odcień niebieskiego, współgrający z ciemnym motywem
val DarkOnSecondary = Color(0xFF000000)     // Ciemny tekst na wtórnym kolorze

//Ratings Colors
val Good = Color(0xFF2E7D32) //Green
val Medium = Color(0xFFFBC02D) //Yellow
val Bad = Color(0xFFD32F2F) //Red

//FavoriteButton colors
val Red = Color(0xFFD32F2F)
val Gray = Color(0xFFAAAAAA)